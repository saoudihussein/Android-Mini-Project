<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dsandroiid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DsAndroiid">
        <activity android:name=".Schedule"></activity>
        <activity android:name=".Meal" />
        <activity android:name=".FoodDay" />
        <activity android:name=".MyCalories" />
        <activity android:name=".Home" />
        <activity android:name=".MaleGender" />
        <activity android:name=".FemaleGender" />
        <activity android:name=".Gender" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>